import { Model_Texture } from "../../../../core/project/model/Texture";
import { State } from "../State";

export class TextureState extends State<Model_Texture> {
  constructor(model: Model_Texture) {
    super(model);
  }
}
